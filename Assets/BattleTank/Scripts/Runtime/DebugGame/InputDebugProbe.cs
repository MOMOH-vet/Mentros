//Зачем: быстрый тест без танка/комбата: выводит Move/Aim/FireHeld в консоль или на экран, чтобы ты сразу видел, что модуль ввода живой.
//Почему полезно: проверяем input изолированно, не смешивая с движением/стрельбой (проще дебажить).

using UnityEngine;


namespace BattleTank.Scripts.Runtime.DebugGame
{
    public class InputDebugProbe : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
